package dev.governance.android.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Oak & Sparrow brand accent palette. Used for trust-state
 * indicators, progress bars, and branded accent surfaces — not
 * for backgrounds. Backgrounds are stock Material 3.
 */
object BloomPalette {
    val PetalCyan = Color(0xFF5FA8D3)
    val TrustGreen = Color(0xFF4ADE80)
    val WarnAmber = Color(0xFFFBBF24)
    val DangerRed = Color(0xFFEF4444)
}
